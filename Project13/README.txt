angular-route.min.js: Library for $routeProvider object

$routeProvider.when: Use for route to pages

$routeProvider.otherwise: Use for default route (homepage)

ng-view: Html element where content page render

Each page can have it is own controller

Template can write html directly:
- template : "<h1>Main</h1><p>Click on the links to change this content</p>"