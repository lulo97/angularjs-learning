ng-app = Define AngularJS application
ng-controller = The controller of application
$scope = Main object of application, parameter of controller

An app can have mutiple controller
- All controller shared the same $rootScope

An AngularJS application have 3 part:
- View: Html file
- Model: $scope, data
- Controller: Javascript code to CRUD data

